[package]
name = "rust-module-example"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"